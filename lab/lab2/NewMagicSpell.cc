#include <iostream>
#include <climits>
#include <cmath>
#include <vector>
#include <string>
using namespace std;

int main() {
    short n;
    cin >> n;

    vector<vector<int> > pos(n,vector<int>(2));
    for(short i = 0;i<n;i++){
        cin >> pos.at(i).at(0) >> pos.at(i).at(1);
    }
    string s;
    cin >> s;//for equation

    // for(short i = 0;i<n;i++){
        for(short j = 0;j<s.size();j++){
            
        }
    // }



}